/*
 * Forager keymap built on urob's ZMK system (QWERTY alphas).
 *
 * This is a thin wrapper: it selects the 34-key position labels and pulls in
 * the shared base keymap. All behaviors, combos, layers, and tuning live in
 * base.keymap / combos.dtsi / mouse.dtsi / leader.dtsi.
 */

#define CONFIG_WIRELESS

#include "zmk-helpers/key-labels/34.h"
#include "base.keymap"
